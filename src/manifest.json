/*
    文件原始模版： http://open.chrome.360.cn/extension_dev/getstarted.html
        但模版可能有点旧，并不兼容现有的Chrome版本（比如 manifest_version 和 popup 就有差异）。
    所以请直接使用下面的改良版~
*/
{
    // 必须的字段
    "name": "跨域iframe通讯方案",
    "version": "1.0",
    "manifest_version": 2,  // 值至少为2

    // 建议提供的字段
    "description": "这是一个 解决父子页面（如iframe）的跨域通讯的插件，让你可以获得控制当前页面的子页面的内容。",
    // "icons": { },
    // "default_locale": "en",

    // 多选一，或者都不提供
    "browser_action": {
        "default_icon": "icon.png",
        "default_popup": "popup.html"
    },
    // "page_action": { },
    // "theme": { },
    // "app": { },

    // 根据需要提供
    "permissions": [
        "tabs",

        // 匹配任何http协议的URL
		"http://*/*",
        // 匹配任何https协议的URL
		"https://*/*"
    ]
}